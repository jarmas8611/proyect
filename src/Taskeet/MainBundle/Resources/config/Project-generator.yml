generator: admingenerator.generator.doctrine
params:
    model: Taskeet\MainBundle\Entity\Project
    namespace_prefix: Taskeet
    bundle_name: MainBundle
    i18n_catalog: TaskeetMainBundle
    fields:
        tickets:
            label: Tareas
            dbType:           collection
            formType:         collection
            addFormOptions:
                widget:       table
                type:         \Taskeet\MainBundle\Form\Type\Ticket\EditType
                allow_add:    true
                allow_delete: true
                by_reference: false
                options:
                    label:          Nuevo ticket
                    data_class:     Taskeet\MainBundle\Entity\Ticket

builders:
    list:
        params:
            title: Proyectos
            display: [name, category, client, startDate, dueDate]
            actions:
                new: ~
            object_actions:
                show: ~
                edit: ~
                delete: ~
    filters:
        params:
            display: [name, category, client, startDate, dueDate, departments]
    new:
        params:
            title: Nuevo proyecto
            fields:
                startDate:
                    formType: datepicker
                dueDate:
                    formType: datepicker
            tabs:
                "General":
                    "NONE": [[name, category, client, startDate, dueDate, description], [tickets]]
                "Extras":
                    "Otros": [departments, members]
            actions:
                save: ~
                save-and-add: ~
                save-and-list: ~
                list: ~
    edit:
        params:
            title: "Editando el proyecto \"%object%\"|{ %object%: Project.name }|"
            credentials: 'hasRole("ROLE_SUPER_ADMIN") or hasPermission(object, "OPERATOR")'
            fields:
                startDate:
                    formType: datepicker
                dueDate:
                    formType: datepicker
            tabs:
                "General":
                    "NONE": [name, category, client, startDate, dueDate, description, [tickets]]
                "Extras":
                    "Otros": [departments, members]
            actions:
                save: ~
                save-and-add: ~
                save-and-list: ~
                list: ~
                show: ~
    show:
        params:
            title: "Proyecto \"%object%\"|{ %object%: Project.name }|"
            display: [name, category, client, startDate, dueDate, description, departments, members, tickets]
            actions:
                new: ~
                list: ~
#                edit: ~
    delete:
        params:
            credentials: 'hasRole("ROLE_SUPER_ADMIN") or hasPermission(object, "OWNER")'
